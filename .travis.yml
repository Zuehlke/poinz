language: node_js
node_js:
  - "4.2"
  - "5.7"
cache:
  directories:
    - node_modules
    - client/node_modules
    - server/node_modules
install:
  - npm i
  - cd $TRAVIS_BUILD_DIR/client && npm i
  - cd $TRAVIS_BUILD_DIR/server && npm i
  - cd $TRAVIS_BUILD_DIR/

